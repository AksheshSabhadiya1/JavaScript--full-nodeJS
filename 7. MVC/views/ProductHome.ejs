
<%- include('partials/head') %>
</head>
<body class="bg-gradient-to-r from-blue-400 to-purple-600 min-h-screen flex flex-col items-center text-white font-sans">
  <%- include('partials/header') %> 
  <h1 class="text-5xl font-extrabold drop-shadow-lg">Welcome to Product Cart 🛍️🛒</h1>
  <main>
    <div class="max-w-3xl mx-auto p-8 bg-gray-50 rounded-lg shadow-2xl">
      <h3 class="text-3xl font-semibold text-center text-indigo-700 mb-8">🛒 Registered Products</h3>
      <ul class="grid grid-cols-1 sm:grid-cols-5 gap-4">
        <% registarProducts.forEach( product => { %>
          <li class="bg-white text-black shadow-lg rounded-2xl p-4 font-bold hover:shadow-2xl hover:bg-indigo-100 transition-transform transform hover:scale-105">
            <div class="flex flex-col items-center">
              <img src="<%= product.productImage %>" alt="<%= product.productName %>" class="w-32 h-32 object-cover rounded-lg mb-4 border-4 border-indigo-300 aspect-square" width="320px">
              <h4 class="text-2xl text-indigo-700 mb-2"><%= product.productName %></h4>
              <p class="text-lg text-gray-700">💰 Price: ₹<%= product.productPrice %></p>
              <p class="text-lg text-yellow-500">⭐ Rating: <%= product.productRating %>/5</p>
            </div>
          </li>
        <% }) %>
      </ul>
    </div>
           
  </main>
</body>
</html>
<!--  -->