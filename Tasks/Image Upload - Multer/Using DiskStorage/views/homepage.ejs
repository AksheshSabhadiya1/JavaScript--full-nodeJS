<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Uploader</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background: linear-gradient(100deg, #6a11cb, #2575fc); /* Gradient background */
      color: #333;
    }

    .upload-container {
      background-color: rgba(255, 255, 255, 0.9); /* Slightly transparent white */
      padding: 3rem;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      text-align: center;
      width: 90%;
      max-width: 500px;
    }

    .upload-container h1 {
      color: #333;
      margin-bottom: 1.5rem;
    }

    .upload-form {
      display: flex;
      flex-direction: column;
      align-items: stretch;
    }

    .file-input {
      padding: 15px;
      margin-bottom: 2rem;
      border: 2px dashed #bbb;
      border-radius: 8px;
      background-color: #f9f9f9;
      cursor: pointer;
      transition: border-color 0.3s ease;
    }

    .file-input:hover {
      border-color: #888;
    }

    .upload-button {
      padding: 15px 25px;
      background-color: #009688; /* Teal */
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s ease;
    }

    .upload-button:hover {
      background-color: #045c52;
    }

    /* Responsive adjustments */
    @media (max-width: 600px) {
      .upload-container {
        padding: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="upload-container">
    <h1>Upload Your Image</h1>
    <form action="/upload" method="POST" enctype="multipart/form-data" class="upload-form">
      <input type="file" name="profileImage" accept="image/*" class="file-input">
      <button type="submit" class="upload-button">Submit Image</button>
    </form>
  </div>
</body>
</html>